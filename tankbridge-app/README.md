# Tankbridge

Supabase에 연결된 실제 배포용 버전입니다. 사용법은 `tankbridge-launch-guide.md` 문서를 따라오시면 됩니다.

## 로컬에서 미리 보기 (선택 사항)
```
npm install
npm run dev
```
`.env.example`을 복사해 `.env`로 만들고 본인의 Supabase 값을 채워넣어야 정상 동작합니다.

## 배포
Vercel / Netlify에 이 폴더를 그대로 올리면 됩니다. 자세한 순서는 `tankbridge-launch-guide.md`를 참고하세요.
